# DESIGN.md

本文件定义 H5 营销编辑器当前版本的产品视觉、布局和交互基线。后续页面开发和高保真校准以本文件、最新设计稿和 `src/styles/design-system.css` 为准。

## 1. 产品定位

产品定位为桌面端 H5 营销可视化搭建平台，核心目标是让用户通过模板、素材和组件快速完成营销页创建、预览和发布。

整体视觉关键词：轻量、专业、高信息密度但不拥挤、白色内容面板、浅灰蓝工作区、品牌蓝强调、弱阴影、清晰边界。

当前产品不包含数据分析和团队协作模块。

## 2. 当前页面信息架构

```text
登录
  ↓
工作台 ─ 模板中心 ─ 我的作品 ─ 素材管理
  ↓
H5 编辑器
  ├─ 组件库
  ├─ 页面管理
  ├─ 模板快捷入口
  ├─ 素材快捷入口
  ├─ 属性编辑
  ├─ 样式编辑
  ├─ 交互编辑
  └─ 动画编辑
  ↓
预览 → 发布设置 → 发布完成
```

## 3. 平台级导航

工作台、模板中心、我的作品、素材管理统一使用顶部导航，不使用左侧平台级侧边栏。平台页顶部导航与 H5 编辑页 `Toolbar.vue` 使用同一套视觉语言。

顶部导航顺序固定为：

`工作台 / 模板中心 / 我的作品 / 素材管理`

规则：

- 高度固定为 `64px`，与编辑器顶部导航一致。
- 左侧为产品 Logo、名称和副标题，Logo 尺寸与编辑器一致。
- 中部四个一级导航均使用“图标 + 文字”，图标尺寸约 `14–16px`。
- 导航按钮高度 `36px`，圆角使用 `--radius-md`。
- Default 使用 `--text-secondary`。
- Hover 使用 `--brand-50` 背景与 `--brand-600` 文字。
- 当前页面增加浅品牌背景、品牌蓝文字与底部 2px 指示条。
- 右侧依次为全局搜索、帮助、通知、分隔线、头像和用户入口。
- 禁止重新增加“数据分析”“团队协作”等一级导航。
- 平台导航与编辑器导航不得分别维护两套颜色、Logo 尺寸或 Hover 规则。

### 3.1 搜索组件

平台搜索框统一使用 `ds-search-field`，搜索区属于导航右侧操作区，而不是独立灰色容器。

- 外层和输入区域必须是同一个白色 Surface。
- 禁止出现“外层灰色、内部 input 白色”的双层背景。
- 输入框本身必须透明，只由容器提供背景、边框和圆角。
- 标准高度 `36px`。
- 桌面宽度上限约 `320px`，允许随可用空间收缩，不应挤压一级导航。
- Default：白底 + `--border-default`。
- Hover：`--border-strong`。
- Focus：品牌蓝边框 + `--focus-ring`。
- 搜索图标、占位文本使用 `--text-muted`。
- 快捷键提示位于输入框右侧，空间不足时优先隐藏快捷键提示。
- 约 `1320px` 以下先缩短搜索框并隐藏用户副信息。
- 约 `900px` 以下可隐藏全局搜索；一级导航切换为仅图标模式，而不是把导航移到左侧。

## 4. 平台级页面

### 4.1 登录页

- 左侧为登录卡片，右侧展示产品价值和 H5 页面示意。
- 登录卡片保持 380–420px 宽度。
- 输入框高度约 40px。
- 主按钮使用品牌蓝。
- 页面背景为浅蓝渐变，不使用深色大面积背景。

### 4.2 工作台

- 顶部为用户问候、新建作品和从模板创建。
- 状态卡片只展示：我的作品、已发布、草稿、素材。
- 禁止展示访问量、访客数、转化率等分析指标。
- 主体展示最近作品，以及模板中心、素材管理快捷入口。
- 页面卡片和模块保持紧凑，避免无必要的大 Hero 占用首屏。

### 4.3 我的作品

筛选：全部 / 已发布 / 草稿 / 回收站。

列表字段：作品名称 / 状态 / 页面数 / 更新时间 / 操作。

操作：编辑 / 复制 / 删除。

禁止出现“数据”“数据分析”“访问量”“转化率”入口。

### 4.4 模板中心

- 采用顶部页面标题 + 搜索，主体在白色面板内完成分类和模板浏览。
- 不使用大面积营销 Hero。
- 支持分类筛选、全部模板 / 我的收藏 / 我的模板。
- 卡片 Hover 显示预览和使用模板操作。
- 收藏状态即时反馈。
- 模板预览使用大弹层，不直接覆盖编辑内容。
- 使用模板前必须明确提示会替换当前页面。

### 4.5 素材管理

- 分类：图片 / 图标 / 插画 / 视频 / 音频 / 我的上传。
- 支持搜索、上传和素材网格。
- 素材卡片保持统一比例。
- Hover 显示轻量操作层。
- 上传卡使用虚线边框。

## 5. H5 编辑器布局

编辑器固定结构：

```text
全局导航
编辑工具栏
┌────────────┬────────────┬──────────────────────┬──────────────┐
│ 组件功能 Rail │ 组件/模板面板 │ 页面管理 + 中央设备画布 │ 右侧属性面板 │
└────────────┴────────────┴──────────────────────┴──────────────┘
```

核心原则：页面管理位于画布左侧；手机位于中央工作区；右侧属性面板固定；画布缩放只影响设备预览。

## 6. 手机预览

手机外观采用最新设计稿中的黑色设备：黑色边框、大圆角、顶部 Dynamic Island / 刘海区域、摄像头与听筒细节、左右实体键细节、屏幕内容独立滚动。手机设计尺寸以 390px 宽度级别为基准，缩放范围 50%–150%。

## 7. 页面管理

页面卡片支持单击切换、双击重命名、Hover 操作、上移、下移、复制、删除和新增。至少保留一个页面。

状态：Default 为白色或透明背景；Hover 为浅蓝灰背景；Active 为品牌蓝描边 + 浅蓝底；Disabled 降低透明度并禁止点击。

## 8. 组件选中与操作

Hover：1px 品牌蓝虚线，不增加大面积蓝色覆盖。

Selected：2px 品牌蓝实线 + 8 个白底蓝边控制点。

手机右侧纵向操作条固定为：删除 / 上移 / 下移。第一项禁用上移，最后一项禁用下移；删除使用危险色 Hover。

## 9. 右侧属性面板

固定 Tab：内容 / 样式 / 交互 / 动画，不得恢复“数据”Tab。

表单控件高度使用 Compact 28px、Default 34px、Large 40px。折叠组支持真实展开 / 收起，字段需覆盖 Hover / Focus / Disabled / Error。

## 10. 弹层

普通确认弹层 420–520px，复杂业务弹层 560–760px。统一使用 12–16px 圆角、浅遮罩、Header / Body / Footer 分区和 160–220ms 动效。

重要弹层包括：删除确认、模板预览、AI 生成、移动预览、发布流程。

## 11. 发布流程

发布采用三阶段：基础信息 / 发布设置 / 发布完成。支持作品名称、访问路径、公开访问、密码访问、发布检查、二维码、复制访问链接和返回工作台。

## 12. Design Token

代码唯一来源：`src/styles/design-system.css`。

新增样式优先使用 `--brand-*`、`--surface-*`、`--text-*`、`--border-*`、`--radius-*`、`--shadow-*`、`--focus-ring` 与 motion token。不要在新组件中重新发明品牌蓝、通用边框和通用阴影。

## 13. 响应式

编辑器：`>=1440px` 完整布局，`1280–1439px` 压缩侧栏与间距，`<1280px` 优先提示桌面端。

平台页面：1440px 优先 4 列，1200px 3 列，1024px 2 列，更窄时按内容降级；顶部一级导航始终保留结构，空间不足时可切换为仅图标表现。

## 14. 设计校准顺序

高保真还原按以下顺序：布局结构 → 模块尺寸 → 间距 → 字体层级 → 颜色 → 边框与圆角 → Hover / Selected / Focus / Disabled → 阴影与微动效 → 响应式。

## 15. 开发前检查

新增页面或复杂交互前确认：最新设计稿是否覆盖、是否已有可复用组件和 Token、是否需要 Empty / Loading / Error、是否影响拖拽选择和缩放、是否引入已取消产品模块。

若设计稿未覆盖复杂页面，先出设计稿，再进入代码实现。
