# DESIGN.md

本文件定义 H5 营销编辑器当前版本的产品视觉、布局和交互基线。后续页面开发和高保真校准以本文件、最新设计稿和 `src/styles/design-system.css` 为准。

## 1. 产品定位

产品定位为桌面端 H5 营销可视化搭建平台，核心目标是让用户通过模板、素材和组件快速完成营销页创建、预览和发布。

整体视觉关键词：

- 轻量
- 专业
- 高信息密度但不拥挤
- 白色内容面板
- 浅灰蓝工作区
- 品牌蓝强调
- 弱阴影、清晰边界
- 小圆角控件 + 中等圆角卡片

当前产品不包含数据分析和团队协作模块。

## 2. 当前页面信息架构

```text
登录
  ↓
工作台
  ├─ 我的作品
  ├─ 模板中心
  └─ 素材管理
       ↓
H5 编辑器
  ├─ 组件库
  ├─ 页面管理
  ├─ 模板快捷入口
  ├─ 素材快捷入口
  ├─ 属性编辑
  ├─ 样式编辑
  ├─ 交互编辑
  └─ 动画编辑
       ↓
预览
       ↓
发布设置
       ↓
发布完成
```

## 3. 平台级页面

### 3.1 登录页

- 左侧为登录卡片，右侧展示产品价值和 H5 页面示意。
- 登录卡片保持 380–420px 宽度。
- 输入框高度约 40px。
- 主按钮使用品牌蓝。
- 微信 / 验证码等辅助方式采用弱化图标按钮。
- 页面背景为浅蓝渐变，不使用深色大面积背景。

### 3.2 工作台

顶部信息包括：

- 当前用户问候
- 新建作品
- 从模板创建

状态卡片仅展示项目管理相关信息：

- 我的作品
- 已发布
- 草稿
- 素材

禁止展示访问量、访客数、转化率等分析指标。

工作台主体展示最近作品，以及模板中心、素材管理快捷入口。

### 3.3 我的作品

顶部：页面标题 + 新建作品。

筛选：

- 全部
- 已发布
- 草稿
- 回收站

列表字段：

- 作品名称
- 状态
- 页面数
- 更新时间
- 操作

操作：编辑 / 复制 / 删除。

禁止出现“数据”“数据分析”“访问量”“转化率”入口。

### 3.4 模板中心

- 支持关键词搜索。
- 支持分类筛选。
- 支持全部模板 / 我的收藏 / 我的模板。
- 卡片 Hover 显示预览和使用模板操作。
- 收藏状态即时反馈。
- 模板预览使用大弹层，不直接覆盖编辑内容。
- 使用模板前必须明确提示会替换当前页面。

### 3.5 素材管理

- 分类：图片 / 图标 / 视频等。
- 支持搜索、上传和素材网格。
- 素材卡片保持统一比例。
- Hover 显示轻量操作层。
- 上传卡使用虚线边框。

## 4. H5 编辑器布局

编辑器固定结构：

```text
全局导航
编辑工具栏
┌────────────┬────────────┬──────────────────────┬──────────────┐
│ 组件功能 Rail │ 组件/模板面板 │ 页面管理 + 中央设备画布 │ 右侧属性面板 │
└────────────┴────────────┴──────────────────────┴──────────────┘
```

核心原则：

- 页面管理位于画布左侧。
- 手机位于中央工作区。
- 右侧属性面板固定，不随缩放变化。
- 画布缩放只影响设备预览。
- 小于 1280px 时优先提示桌面端使用，不强行压缩完整编辑器。

## 5. 手机预览

手机外观采用最新设计稿中的黑色设备：

- 黑色边框
- 大圆角
- 顶部 Dynamic Island / 刘海区域
- 摄像头与听筒细节
- 左右实体键细节
- 屏幕内容独立滚动

手机设计尺寸以 390px 宽度级别为基准，缩放范围 50%–150%。

## 6. 页面管理

页面卡片支持：

- 单击切换
- 双击重命名
- Hover 显示操作
- 上移
- 下移
- 复制
- 删除
- 新增

状态：

- Default：白色 / 透明背景
- Hover：浅蓝灰背景
- Active：品牌蓝描边 + 浅蓝底
- Disabled：降低透明度并禁止点击

至少保留一个页面。

## 7. 组件选中与操作

### Hover

- 1px 品牌蓝虚线
- 不增加大面积蓝色覆盖

### Selected

- 2px 品牌蓝实线
- 8 个白底蓝边控制点

### 选中后操作条

手机右侧显示纵向操作：

1. 删除
2. 上移
3. 下移

规则：

- 未选中时隐藏。
- 第一项禁用上移。
- 最后一项禁用下移。
- 删除使用危险色 Hover。
- 上移 / 下移后保持选中。
- 删除后取消选中。

## 8. 右侧属性面板

固定 Tab：

- 内容
- 样式
- 交互
- 动画

不得恢复“数据”Tab。

表单控件建议高度：

- Compact：28px
- Default：34px
- Large：40px

折叠组支持真实展开 / 收起状态。

所有字段至少具备：

- Hover
- Focus
- Disabled
- Error（需要校验时）

## 9. 弹层

普通确认弹层：420–520px。

复杂业务弹层：560–760px。

统一规则：

- Header / Body / Footer 明确分区
- 12–16px 圆角
- 浅遮罩 + 轻 Blur
- 主按钮靠右
- 危险操作使用危险色
- 160–220ms 动效

当前重要弹层包括：

- 删除确认
- 模板预览
- AI 生成
- 移动预览
- 发布流程

## 10. 发布流程

发布采用三阶段：

1. 基础信息
2. 发布设置
3. 发布完成

支持：

- 作品名称
- 访问路径
- 公开访问
- 密码访问
- 发布检查
- 二维码
- 复制访问链接
- 返回工作台

## 11. Design Token

代码唯一来源：

`src/styles/design-system.css`

新增样式优先使用：

```css
var(--brand-600)
var(--surface-0)
var(--surface-1)
var(--surface-2)
var(--text-strong)
var(--text-primary)
var(--text-secondary)
var(--text-muted)
var(--border-subtle)
var(--border-default)
var(--border-brand)
var(--radius-md)
var(--radius-xl)
var(--shadow-sm)
var(--shadow-lg)
var(--focus-ring)
var(--motion-base)
```

不要在新组件中重新发明品牌蓝、常规边框和通用阴影。

## 12. 响应式

### 编辑器

- `>= 1440px`：完整布局
- `1280–1439px`：压缩左右侧栏和间距
- `< 1280px`：优先桌面端提示

### 平台页面

- 1440px：4 列卡片优先
- 1200px：3 列
- 1024px：2 列
- 更窄时可转单列

## 13. 设计校准顺序

高保真还原时按以下优先级：

1. 布局结构
2. 模块尺寸
3. 间距
4. 字体层级
5. 颜色
6. 边框与圆角
7. Hover / Selected / Focus / Disabled
8. 阴影与微动效
9. 响应式

## 14. 开发前检查

新增页面或复杂交互前确认：

- 最新设计稿是否已覆盖
- 是否已有可复用组件
- 是否已有对应 Design Token
- 是否需要 Empty / Loading / Error
- 是否会影响编辑器拖拽、选择和缩放
- 是否引入已取消的产品模块

若设计稿未覆盖复杂页面，先出设计稿，再进入代码实现。
