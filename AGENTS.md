# AGENTS.md

本文件用于约束 AI Agent、代码助手及自动化开发工具在本仓库中的工作方式。目标是让后续迭代保持产品范围、视觉语言、代码结构和交互行为一致。

## 1. 开发前必读

修改 UI、交互或新增页面前，按顺序阅读：

1. `README.md`
2. `DESIGN.md`
3. `docs/design-system.md`
4. 与目标模块相关的现有 Vue 组件和 Store

不要在未确认现有实现前重建重复组件、重复 Store 或重复样式体系。

## 2. 当前产品范围

当前核心流程：

`登录 → 工作台 → 我的作品 / 模板中心 / 素材管理 → H5 编辑器 → 预览 → 发布`

明确不包含：

- 数据分析
- 团队协作

不要重新添加对应菜单、路由、卡片、按钮、统计指标或占位页面，除非产品范围被明确重新确认。

## 3. UI 开发规则

- 设计稿优先于历史实现。
- 复杂页面或设计稿未覆盖的新交互，先补设计，再编码。
- 优先使用 `src/styles/design-system.css` 中的 Token。
- 禁止在新组件中散落相近但不一致的蓝色、边框色、阴影和圆角值。
- Element Plus 组件应遵循全局 Design Token，不单独覆盖出另一套视觉语言。
- 所有可交互控件至少考虑 Default / Hover / Active / Focus / Disabled。
- 业务表单还要考虑 Error / Success / Loading。
- 列表和数据容器需要 Empty 状态。

## 4. 编辑器核心约束

编辑器结构保持：

`编辑工具栏 → 左侧组件库 → 左侧页面管理 → 中央设备画布 → 右侧属性面板`

关键规则：

- 页面管理位于画布左侧。
- 手机操作条位于设备右侧。
- 选中组件后显示删除 / 上移 / 下移。
- 首组件禁用上移，尾组件禁用下移。
- 画布缩放仅影响设备，不影响侧栏尺寸。
- 属性面板固定为：内容 / 样式 / 交互 / 动画。
- 编辑状态下组件点击优先用于选中；预览状态下才执行配置事件。
- 禁止通过 `eval` 执行 Schema 内自定义代码。

## 5. Schema 与组件系统

- 页面和组件继续使用 JSON Schema 驱动。
- 新组件必须通过统一组件注册表接入。
- 默认 Props / Styles 必须提供稳定默认值。
- 编辑器配置与预览渲染必须使用同一份 Schema。
- 修改 Schema 结构时，检查本地持久化、模板、AI 生成和渲染器兼容性。

## 6. 状态与持久化

- 编辑器状态通过 Pinia Store 管理。
- 对编辑内容的更新应进入历史记录链路，保证撤销 / 重做可用。
- localStorage 中已有页面优先于默认示例页。
- 模板应用、页面切换和删除后需确保当前选中状态合法。

## 7. 代码规范

- Vue 3 Composition API + `<script setup lang="ts">`。
- TypeScript 严格类型优先，避免无必要的 `any`。
- 组件使用 PascalCase。
- composable / 方法使用 camelCase。
- 中文 UI 文案保持简洁一致。
- 避免巨型组件；当同一业务块拥有独立状态和交互时拆分子组件。
- 不为短期需求引入大型依赖。

## 8. 修改流程

每次改动至少执行以下检查：

1. 确认产品范围没有扩张。
2. 检查设计稿和 `DESIGN.md`。
3. 修改代码。
4. 检查 Hover / Focus / Disabled / Empty / Error。
5. 检查 1440px 桌面布局和低高度视口。
6. 执行 `pnpm build`。
7. 检查 GitHub Actions `Build & Preview`。

## 9. 禁止事项

- 不重新引入数据分析入口。
- 不重新引入团队协作入口。
- 不使用静态截图替代本应可交互的编辑器功能。
- 不绕过 Store 直接维护另一份页面 Schema。
- 不在多个页面重复复制同一设计 Token。
- 不为了视觉效果破坏拖拽、选择、缩放、预览或发布功能。

## 10. 提交约定

推荐提交前缀：

- `feat:` 新功能
- `fix:` Bug 修复
- `refactor:` 重构
- `style:` 纯视觉调整
- `docs:` 文档
- `chore:` 工程配置

当前仓库直接在 `main` 上持续开发时，每个提交保持单一、可回滚的变更主题。
